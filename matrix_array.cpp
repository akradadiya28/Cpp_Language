#include <iostream>
using namespace std;
int main()
{
    int row, col, i;

    int a[row][col];

    cout << "Enter the array's row size: ";
    cin >> row;

    cout << "Enter the array's column size: ";
    cin >> col;

    return 0;
}